
  # NexusFlow

  This is a code bundle for NexusFlow. The original project is available at https://www.figma.com/design/ooM4lRx2dgmiqOaauCweC5/NexusFlow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  